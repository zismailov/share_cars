Hello <%= @trip.name%>


Information on your ad:
<%= trip_steps_breadcrumb(@trip)%>, the <% = l @trip.departure_date, format: :long%> to <%= @trip.departure_time.try(:strftime, "% k:% m")% >

Confirmation link: <%= url_for confirm_trip_url(@trip.confirmation_token)%>

Change link: <%= url_for edit_trip_url(@trip.edition_token)%>

Removal link: <%= url_for delete_trip_url(@trip.deletion_token)%>