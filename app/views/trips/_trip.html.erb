<div class="trip">
  <%= link_to trip, class: 'trip-item' do %>
    <div class="driver">
      <div class="avatar">
        <%= image_tag('carpooling-avatar-default2.png') %>
      </div>
      <div class="info">
        <div class="name">
          <%= trip.name %>
        </div>
        <div class="age">
          <%= "#{trip.age} years" %>
        </div>
      </div>
    </div>
    <div class="trip-info">
      <div class="title">
        <%= l trip.leave_at, format: :long %>
      </div>
      <div class="details">
        <%= "#{trip.point_from.city}" %>
        &rarr;
        <%= "#{trip.point_to.city}" %>
      </div>
    </div>
    <div class="price">
      <p>
        <%= "#{trip.price} dollars" %>
      </p>
      <div class="seats">
        <%= "#{trip.seats} places" %>
      </div>

    </div>
    <div class="clearfix"></div>
  <% end %>
</div>
